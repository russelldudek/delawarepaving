(function initApprovedRoller(){const existing=document.getElementById('roller');const roadWindow=document.querySelector('.road-window');if(!existing||!roadWindow)return;const roller=existing.cloneNode(true);existing.replaceWith(roller);roller.removeAttribute('style');const rearWheel=roller.querySelector('#rearWheel');const frontDrum=roller.querySelector('#frontDrum');if(!rearWheel||!frontDrum)return;const reduced=window.matchMedia&&window.matchMedia('(prefers-reduced-motion: reduce)').matches;let phase=0;let lastTime=performance.now();let lastX=0;let wheelAngle=0;const ease=t=>t<.5?4*t*t*t:1-Math.pow(-2*t+2,3)/2;const travelWidth=()=>{const leftOffset=parseFloat(getComputedStyle(roller).left)||0;return Math.max(0,roadWindow.clientWidth-leftOffset-roller.offsetWidth-12)};const setPosition=(x,bob=0)=>{roller.style.setProperty('--approved-roller-x',`${x}px`);roller.style.transform=`translate3d(${x}px,${bob}px,0)`;rearWheel.style.transform=`rotate(${wheelAngle}deg)`;frontDrum.style.transform=`rotate(${wheelAngle*.72}deg)`};if(reduced){setPosition(travelWidth()*.5,0);return}function frame(now){const dt=Math.min(40,now-lastTime);lastTime=now;phase=(phase+dt*.000125)%2;const outbound=phase<=1;const local=outbound?phase:2-phase;const x=ease(local)*travelWidth();const dx=x-lastX;lastX=x;wheelAngle+=dx*1.48;setPosition(x,Math.sin(now*.006)*.55);window.requestAnimationFrame(frame)}window.requestAnimationFrame(frame)})();